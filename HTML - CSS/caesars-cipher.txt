function rot13(str) {
  let regex = /[A-Z]/g;
  return str.replace(/[A-Z]/g, (match) => {
    let char = match.charCodeAt(0)-13
     console.log(match.charCodeAt(0));
    return String.fromCharCode(
      match.charCodeAt(0)-13 >= 65
      ? match.charCodeAt(0)-13
      : match.charCodeAt(0)+13
      )
  })
}
rot13("SERR PBQR PNZC");